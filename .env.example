NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Simple shared-password gate for the whole dashboard (see middleware.ts).
# Both are required for the login to work; without them the app blocks
# all access rather than silently staying open.
DASHBOARD_PASSWORD=
DASHBOARD_AUTH_SECRET=
